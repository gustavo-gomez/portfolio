import {Barlow, Barlow_Condensed, Kanit} from "next/font/google";

export const barlow = Barlow({subsets: ['latin'], weight: "500"});
export const barlowCondensed = Barlow_Condensed({subsets: ['latin'], weight: "500"});
export const kanit = Kanit({subsets: ['latin'], weight: "500"});

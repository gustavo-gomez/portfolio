import Title from "@/app/components/Title";

export const Projects = () => {

  return (
    <section id='projects' className="text-white section">
      <Title title={'Proyectos'}/>
    </section>
  )
}